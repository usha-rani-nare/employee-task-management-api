# DO NOT commit real values here. These are base64 placeholders.
# In practice this gets created via:
#   kubectl create secret generic employee-api-secret \
#     --from-literal=DB_USER=<actual-user> \
#     --from-literal=DB_PASSWORD=<actual-password>
# or pulled from Azure Key Vault via CSI driver in a real environment.
apiVersion: v1
kind: Secret
metadata:
  name: employee-api-secret
type: Opaque
data:
  DB_USER: cm9vdA==       # "root" - placeholder only
  DB_PASSWORD: cm9vdA==   # "root" - placeholder only
